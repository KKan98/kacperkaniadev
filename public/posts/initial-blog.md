# My First Angular Blog Post

This is a **dummy blog post** written in Markdown.

It contains some basic formatting so I can test my Angular Markdown renderer.

## Introduction

Markdown is surprisingly convenient for writing blog posts.

You can use **bold text**, *italic text*, ~~strikethrough~~ and `inline code`.

> This is a blockquote.
>
> It can contain multiple lines.

## Code

Here is some TypeScript:

```typescript
export function greet(name: string): string {
  return `Hello, ${name}!`;
}
```
